[pytest]
# ignore the RuntimeWarning from pandas (and numpy) which is supposed to
# ignore NaNs for nanmedian or nanmean encounters a NaN value.  Instead,
# this RuntimeWarning is being issued. Suppress this as there is no
# error with generating ECLV plot with the CTC and PCT linetypes.
filterwarnings=
   ignore::RuntimeWarning
