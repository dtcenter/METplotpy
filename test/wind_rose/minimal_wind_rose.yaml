# minimal config file to use all the settings in the wind_rose_default.yaml file.
# **NOTE**: update the stat_input and plot_filename settings with the full path to your
# input file and output file, respectively.
stat_input: !ENV '${TEST_DIR}/point_stat_mpr.txt'
plot_filename: !ENV '${TEST_OUTPUT}/wind_rose_minimal.png'

points_path: !ENV '${TEST_OUTPUT}/minimal'